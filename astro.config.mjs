import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 自分のGitHubアカウント名とリポジトリ名に合わせて書き換えてください
  site: 'https://nikonosfive.github.io',
  base: '/Geodic',
});